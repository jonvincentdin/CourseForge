# DATABASE.md

## Database technology

PostgreSQL, accessed via **Drizzle ORM** (`drizzle-orm/postgres-js`),
schema-first, SQL migrations generated by `drizzle-kit`.

**Why Drizzle and not Prisma:** the product brief doesn't mandate an
ORM. Prisma's engine binaries need to download from
`binaries.prisma.sh` at install/generate time; that domain wasn't
reachable in the sandbox this was built in, so it couldn't be
verified to work. Drizzle is pure TypeScript with no native binary
step. This was more than a theoretical concern — the schema in this
file has been pushed to and exercised against a real, locally-running
Postgres 16 instance (`drizzle-kit push`), and every table below has
had real rows written and read through the app's actual API routes.

## Models/tables

### `user` (Milestone 1)
| Column | Type | Notes |
|---|---|---|
| id | uuid, PK | `defaultRandom()` |
| name | text, nullable | |
| email | text, unique, not null | lowercased before insert |
| email_verified | timestamp, nullable | reserved for future email verification flow; unused |
| image | text, nullable | |
| password_hash | text, nullable | bcrypt, 12 rounds; null for future OAuth-only accounts |
| created_at | timestamp, default now | |
| updated_at | timestamp, default now | not yet auto-updated on write — see Known Gaps |

### `account`, `session`, `verification_token` (Milestone 1)
Standard Auth.js `DrizzleAdapter` shape. Not hand-rolled — table
columns must match `@auth/drizzle-adapter`'s expectations exactly if
you touch them.

### `syllabus` (Milestone 2)
| Column | Type | Notes |
|---|---|---|
| id | uuid, PK | |
| user_id | uuid, FK → `user.id`, cascade delete | |
| title | text | defaults to the detected document title, or the filename |
| original_filename | text | |
| storage_key | text | opaque key into `src/lib/storage.ts`, not a raw filesystem path the client ever sees |
| file_size_bytes | integer | |
| status | enum: `processing` / `needs_review` / `ready` / `failed` | |
| extracted_text | text, nullable | full raw PDF text, kept for reprocessing without re-uploading |
| extraction_warnings | text[], nullable | persisted so the review page can show them even on a later visit, not just right after upload |
| processing_error | text, nullable | user-facing message when `status = failed` |
| created_at / updated_at | timestamp | |

**Versioning:** each upload is a fully independent row — no
parent/lineage link between re-uploads. See `DECISIONS.md` for why
this satisfies the brief without a more complex version-graph model.

### `subject` (Milestone 2)
| Column | Type | Notes |
|---|---|---|
| id | uuid, PK | |
| syllabus_id | uuid, FK → `syllabus.id`, cascade delete | |
| name | text | |
| code | text, nullable | e.g. `CS201` |
| description | text, nullable | not populated by extraction yet; available for manual/future use |
| academic_year | integer | 1–4, enforced at the API validation layer (`zod`), not a DB constraint |
| semester | enum: `1st Semester` / `2nd Semester` / `Summer` | |
| units | text, nullable | |
| sort_order | integer, default 0 | preserves detection order within a year/semester group |
| auto_detected | boolean, default true | flips to `false` the moment a human edits or manually adds a row — lets the UI distinguish "CourseForge guessed this" from "a person confirmed/wrote this" |
| created_at / updated_at | timestamp | |

## Relationships

- `account.user_id` / `session.user_id` / `syllabus.user_id` → `user.id`, cascade delete
- `subject.syllabus_id` → `syllabus.id`, cascade delete (verified: deleting a syllabus removes its subjects)

## Indexes

`user.email` has a unique constraint (and therefore an index). No
other indexes yet — no query pattern has needed one at this scale.
Revisit `subject.syllabus_id` and `syllabus.user_id` if listing pages
get slow with real data volume.

## Storage

Original PDFs are stored via `src/lib/storage.ts` (local filesystem
today; see `DECISIONS.md`), keyed by `syllabus.storage_key`, not
inside Postgres itself.

## Not yet modeled (owned by later milestones — do not invent these tables early)

- Course / CourseModule / Quiz / QuizQuestion (Milestone 4)
- CourseProgress (Milestone 7)
- AIProviderConfiguration (Milestone 6)
- CourseShare / CoursePermission / CourseInvitation (Milestone 9)
- Course-copy fields (`source_course_id`, `source_type`) on `Course` (Milestone 9)

## Known gaps

- `user.updated_at` (and `syllabus`/`subject`'s, outside of the
  explicit `updatedAt: new Date()` sets already in the mutation code)
  is not auto-updated by a DB-level trigger — every write path that
  needs it currently sets it explicitly in application code. Fine for
  now; would be worth a DB-level `onUpdate` if more write paths appear
  without remembering to set it.
- No migration files have been generated yet (`npm run db:generate`) —
  Milestone 1 and 2 schema were both applied via `drizzle-kit push`
  directly during verification. Generate real migration files before
  this is used anywhere beyond a single developer's local database.
